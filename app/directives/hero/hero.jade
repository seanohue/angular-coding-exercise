gs-status-bar(ng-model='dm.state.connection')
div.container-fluid
  div.row
    div.col-md-6.col-md-push-3
      div.profile(ng-if='dm.state.connection.complete')
        h1 {{dm.character.name}}
        p {{dm.describe()}}
        img(ng-src='{{dm.portrait()}}')
      a.back(ng-click="dm.back()") Back to Index
      

       

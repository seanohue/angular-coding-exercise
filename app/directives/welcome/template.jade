div.grid-frame.vertical
  div.primary.title-bar
    // @TODO add additional logic to handle warning state while checking API
    div.status(ng-class="dm.state.connection.success ? 'status--success' : 'status--danger'")
      span.status__indicator
      span.status__label(ng-if='!dm.state.connection.complete') Attempting to connect to the Marvel API.
      span.status__label(ng-if='dm.state.connection.success') Successfully connected to the Marvel API.
      span.status__label(ng-if='dm.state.connection.error') There was an error reaching the Marvel API.
  div.grid-block
    div.medium-12.grid-block
      div.welcome.grid-content
        h1.welcome__title
          span(ng-if='dm.state.connection.error') Something went wrong while bootstrapping the {{ dm.title }}
          span(ng-if='dm.state.connection.success') Time to get crackin' on the {{ dm.title }}
